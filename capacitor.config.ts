import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.cifrarepertorio',
  appName: 'CIFRA-REPERTORIO',
  webDir: 'dist'
};

export default config;
